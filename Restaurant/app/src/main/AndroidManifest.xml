<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.hs_weingarten.restaurant" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Restaurant"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Speisenauswahl" >
        </activity>
        <activity
            android:name=".getraenke_view"
            android:label="@string/title_activity_getraenke_view"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".vorspeise_view"
            android:label="@string/title_activity_vorspeise_view"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity android:name=".Bestellung" >
        </activity>
    </application>

</manifest>
